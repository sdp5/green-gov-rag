name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
      if: github.event_name != 'pull_request'

    - name: Build and push Backend
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./deploy/docker/backend.Dockerfile
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ secrets.DOCKER_USERNAME }}/greengovrag-backend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push Frontend
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./deploy/docker/frontend.Dockerfile
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ secrets.DOCKER_USERNAME }}/greengovrag-frontend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Add deployment to cloud provider here
    # Uncomment and configure for your target platform

    # - name: Deploy to AWS ECS
    #   uses: aws-actions/amazon-ecs-deploy-task-definition@v1
    #   with:
    #     task-definition: deploy/aws/task-definition.json
    #     service: greengovrag-service
    #     cluster: greengovrag-cluster

    # - name: Deploy to Azure
    #   uses: azure/webapps-deploy@v2
    #   with:
    #     app-name: greengovrag
    #     images: ${{ secrets.DOCKER_USERNAME }}/greengovrag-backend:latest

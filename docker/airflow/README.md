## Run Airflow Locally

```bash
cd greengovrag/docker
docker compose -f docker-compose.airflow.yml up --build
```

Airflow UI will be available at: http://localhost:8080
<br>Login with: `admin / admin`
